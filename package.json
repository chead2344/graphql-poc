{
  "name": "graphql-poc",
  "version": "1.0.0",
  "description": "A simple GraphQL PoC for a Traders service using Apollo federation",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --transpile-only --no-notify src/index.ts",
    "build": "tsc",
    "generate": "ts-node --transpile-only src/schema",
    "pretest": "npm run generate",
    "test": "jest"
  },
  "author": "Chris Head <c.head@shell.com>",
  "license": "UNLICENSED",
  "dependencies": {
    "@apollo/server": "^4.3.0",
    "@apollo/utils.keyvaluecache": "^2.1.0",
    "@azure/storage-blob": "^12.12.0",
    "dotenv": "^16.0.3",
    "graphql": "^16.6.0",
    "nexus": "^1.3.0"
  },
  "devDependencies": {
    "@types/jest": "^29.2.5",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.4"
  }
}
